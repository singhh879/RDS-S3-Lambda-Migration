# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# NEVER commit this — it contains RDS credentials and account-specific values
terraform.tfvars

# OS files
.DS_Store
Thumbs.db
